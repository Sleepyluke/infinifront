Keep the attached creature EXACTLY — same design, colors, proportions, and ¾ top-down view facing the bottom of the frame. Draw a single horizontal SPRITE STRIP of 4 animation frames of a WALK CYCLE: the identical creature at identical size and screen position in every frame, only its legs and a slight body bob changing to show one smooth walking stride (frame 1 = contact pose legs spread, frame 2 = passing pose body lifted, frame 3 = opposite contact, frame 4 = opposite passing). Exactly 4 equal-width cells in one horizontal row, evenly spaced, each cell framed identically. Plain SOLID magenta #FF00FF background everywhere including the gaps between cells. Clean dark outline, flat cel shading, no blur, matching the reference's exact art style and palette. NO grid lines, NO numbers, NO text, NO drop shadows between frames.
